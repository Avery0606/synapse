const template = `
请根据当前会话，判断项目内是否有相关的 AGENTS.md 文件需要更新或优化。

请重点关注以下几个方面：
当前会话
1. 是否有关键目录变更
2. 是否有关键业务知识新增或变更
3. 是否有新增项目开发指南
4. 是否有新增关键约束
5. 是否有可补充的注意事项

如果有多个 AGENTS.md 需要变更，请逐一处理每个文件，每完成一个的确认后再处理下一个。

优化前请和用户确认优化内容，确认后方可进行修改。确认格式如下：

请确认以下 AGENTS.md 更新内容：
文件路径：<path>
1. 业务知识变更：<具体内容>
2. 关键目录补充或变更：<具体内容>
3. 项目开发指南补充：<具体内容>
4. 关键约束变更：<具体内容>
   - 注意：约束点必须使用 [MUST] 或 [MUST NOT] 前缀
   - [MUST] 表示强制要求：必须做到 xxx
   - [MUST NOT] 表示禁止要求：禁止做 xxx
5. 注意事项补充：<具体内容>

确认后方可执行更新。

你将禁止做如下事情：
- 禁止私自往 AGENTS.md 新增或删除章节
- 所有更新内容必须补充到已有章节中，不得新增或删除任何章节
- 禁止添加读代码就能轻易发现的东西（如明显的函数名、直白的功能描述等）
`;

const command = {
  description: "自动优化项目中需要变更的 AGENTS.md 文件",
  template,
};

export default command;
