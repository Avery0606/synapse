const template = `
# 初始化 Oracle.md 代码索引文件

## 执行流程

### 第一步：扫描项目目录

大致扫描项目根目录，识别关键目录或文件。

### 第二步：生成初稿

根据扫描结果，严格按照如下格式生成一份初稿给用户确认：

\`\`\`
- 文件或目录：XXX
  - 描述：XXX
  - 定位关键词：XXX,XXX,XXX
- 文件或目录：XXX
  - 描述：XXX
  - 定位关键词：XXX,XXX,XXX
\`\`\`

### 第三步：用户确认

询问用户是否有其他关键文件需要加入代码索引。

### 第四步：生成 Oracle.md

用户确认后，将内容整理成 Oracle.md 文件，写入项目根目录。

---

**重要规则**：
- 必须先扫描项目，再生成初稿
- 初稿格式必须严格按照示例格式
- 必须等待用户确认后再生成文件
- 如果用户添加新内容，更新后再次确认
`

const command = {
    description: "初始化 Oracle.md 代码索引文件，帮助 Oracle 快速定位项目代码",
    template,
}

export default command
