const template = `
# 初始化/更新 Oracle.md 代码索引文件

## 执行流程

### 第一步：检测 Oracle.md 是否存在

检查项目根目录是否存在 Oracle.md 文件：
- **如果不存在** → 走"初始化流程"
- **如果存在** → 走"更新流程"

---

## 初始化流程（Oracle.md 不存在）

### 第二步：扫描项目目录

大致扫描项目根目录，识别关键目录或文件。

### 第三步：生成初稿

根据扫描结果，严格按照如下模板生成一份初稿给用户确认：

\`\`\`
### 项目快速索引表
- 文件或目录：XXX
  - 描述：XXX
  - 定位关键词：XXX,XXX,XXX
- 文件或目录：XXX
  - 描述：XXX
  - 定位关键词：XXX,XXX,XXX
\`\`\`

### 第四步：用户确认

询问用户是否有其他关键文件需要加入代码索引。

### 第五步：生成 Oracle.md

用户确认后，将内容整理成 Oracle.md 文件，写入项目根目录。

---

## 更新流程（Oracle.md 已存在）

### 第二步：读取现有 Oracle.md

读取项目根目录的 Oracle.md 文件，分析当前的代码索引结构。

### 第三步：确认更新内容

询问用户需要更新哪些内容，例如：
- 新增关键文件/目录
- 删除已不存在的条目
- 修改描述或定位关键词

### 第四步：生成更新初稿

根据用户需求，生成更新后的初稿给用户确认。

### 第五步：更新 Oracle.md

用户确认后，将更新内容写入 Oracle.md 文件。

---

**重要规则**：
- 必须先检测 Oracle.md 是否存在，再决定走初始化还是更新流程
- 初始化流程：必须先扫描项目，再生成初稿
- 更新流程：必须先读取现有内容，了解当前索引结构
- 初稿格式必须严格按照示例格式
- 必须等待用户确认后再生成/更新文件
- 如果用户添加新内容，更新后再次确认
`

const command = {
    description: "初始化或更新 Oracle.md 代码索引文件，帮助 Oracle 快速定位项目代码",
    template,
}

export default command
