const template = `
# 深度生成项目 AGENTS.md 文件

## 执行流程

### 第一步：扫描项目结构

扫描项目根目录和关键子目录，识别项目技术栈和关键目录。

### 第二步：根目录 AGENTS.md 生成

按以下两个章节逐一询问：

#### 2.1 项目业务背景

询问用户：
- "请描述项目的主要业务是什么、解决什么问题？"
- "是否有补充？"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# 项目简介
**项目简要描述**: xxxx
**主要业务**: xxxx
\`\`\`

#### 2.2 重要约束

询问用户：
- "请描述项目的技术栈限制、编码规范、部署要求？"
- "是否有补充的约束信息需要添加？"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# 开发约束
- 约束点1: xxxxx
- 约束点2: xxxxx
- 约束点3: xxxxx
\`\`\`

#### 2.3 整合生成根目录 AGENTS.md

在所有章节都得到用户确认后，将内容整合成完整的 AGENTS.md 文件，写入项目根目录。

### 第三步：关键目录 AGENTS.md 生成

对于识别出的每个关键目录，按以下三个章节逐一询问：

#### 3.1 目录业务背景（针对该目录）

询问用户：
- "请描述该目录的业务职责是什么？"
- "该目录的核心功能是什么？"
- "是否有补充的业务信息？"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# 目录简介
**业务职责**: xxxx
**核心功能**: xxxx
\`\`\`

#### 3.2 重要约束（针对该目录）

询问用户：
- "该目录的技术约束或规范？"
- "该目录的依赖关系或注意事项？"
- "是否有补充的约束信息？"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# 开发约束
- 约束点1: xxxxx
- 约束点2: xxxxx
- 约束点3: xxxxx
\`\`\`

#### 3.3 整合生成该目录的 AGENTS.md

在所有章节都得到用户确认后，将内容整合成完整的 AGENTS.md 文件，写入该目录。

### 第四步：完成总结

完成所有 AGENTS.md 文件生成后，向用户总结：
- 生成了哪些目录的 AGENTS.md 文件
- 每个文件包含的章节
- 提醒用户可以根据需要手动调整

---

**重要规则**：
- 每个章节生成前必须先询问用户，确认后再生成
- 必须等待用户补充信息后再整合内容
- 使用自然语言与用户交互，不要一次性问太多问题
- 如果用户跳过某个章节，直接跳过不要生成
- 文件生成后展示给用户确认内容是否正确
`

const command = {
    description: "深度生成项目根目录及关键目录的 AGENTS.md 文件",
    template,
}

export default command
