const template = `
# 深度生成项目 AGENTS.md 文件

## 执行流程

### 第一步：扫描项目结构

使用 Bash 工具执行以下命令扫描项目：
- 列出根目录文件：!dir /b
- 列出关键子目录（如 src, lib, app, packages 等）：!dir /b /ad
- 识别关键配置文件（如 package.json, tsconfig.json, Cargo.toml, pom.xml 等）

识别出的关键目录可能包括但不限于：
- src/
- lib/
- app/
- packages/
- components/
- services/
- utils/
- api/
- docs/

### 第二步：根目录 AGENTS.md 生成

**询问用户是否需要生成根目录的 AGENTS.md**

如果用户确认需要，则按以下三个章节逐一询问：

#### 2.1 项目业务背景

询问用户：
- "是否需要包含'项目业务背景'章节？"
- "请描述项目的主要业务是什么、解决什么问题、目标用户是谁？"
- "是否有补充？"

用户确认后，生成该章节内容。

#### 2.2 关键文件快速导航

询问用户：
- "是否需要包含'关键文件快速导航'章节？"
- "请描述项目的主要入口文件、核心模块、常用命令？"
- "是否有补充的导航信息需要添加？"

用户确认后，生成该章节内容。

#### 2.3 重要约束

询问用户：
- "是否需要包含'重要约束'章节？"
- "请描述项目的技术栈限制、编码规范、部署要求？"
- "是否有补充的约束信息需要添加？"

用户确认后，生成该章节内容。

#### 2.4 整合生成根目录 AGENTS.md

在所有章节都得到用户确认后，将内容整合成完整的 AGENTS.md 文件，写入项目根目录。

### 第三步：关键目录 AGENTS.md 生成

对于识别出的每个关键目录，逐一询问用户：

**"是否需要为 [目录名] 目录生成 AGENTS.md 文件？"**

如果用户确认需要，则按以下三个章节逐一询问：

#### 3.1 项目业务背景（针对该目录）

询问用户：
- "请描述该目录的业务职责是什么？"
- "该目录的核心功能是什么？"
- "是否有补充的业务信息？"

用户确认后，生成该章节内容。

#### 3.2 关键文件快速导航（针对该目录）

询问用户：
- "该目录的主要入口和导出是什么？"
- "该目录的关键文件和数据结构？"
- "常用的 API 或调用方式？"
- "是否有补充的导航信息？"

用户确认后，生成该章节内容。

#### 3.3 重要约束（针对该目录）

询问用户：
- "该目录的技术约束或规范？"
- "该目录的依赖关系或注意事项？"
- "是否有补充的约束信息？"

用户确认后，生成该章节内容。

#### 3.4 整合生成该目录的 AGENTS.md

在所有章节都得到用户确认后，将内容整合成完整的 AGENTS.md 文件，写入该目录。

### 第四步：完成总结

完成所有 AGENTS.md 文件生成后，向用户总结：
- 生成了哪些目录的 AGENTS.md 文件
- 每个文件包含的章节
- 提醒用户可以根据需要手动调整

---

**重要规则**：
- 每个章节生成前必须先询问用户，确认后再生成
- 必须等待用户补充信息后再整合内容
- 使用自然语言与用户交互，不要一次性问太多问题
- 如果用户跳过某个章节，直接跳过不要生成
- 文件生成后展示给用户确认内容是否正确
`

const command = {
    description: "深度生成项目根目录及关键目录的 AGENTS.md 文件",
    template,
}

export default command
