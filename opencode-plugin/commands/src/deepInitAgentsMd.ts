const template = `
# 深度生成项目 AGENTS.md 文件

---

## 入口判断

首先检测项目根目录是否存在 AGENTS.md 文件：

- **存在** → 进入[创建模式](#创建模式agents-md-不存在时)
- **不存在** → 进入[更新模式](#更新模式agents-md-已存在时)

---

## 创建模式（AGENTS.md 不存在时）

### 第1步：扫描项目结构

扫描项目根目录及所有子目录，识别：
- 项目技术栈（编程语言、框架、构建工具等）
- 关键目录（包含核心业务逻辑、配置文件、测试代码等的目录）
- 目录层级结构和组织方式

扫描完成后，向用户展示识别出的关键目录列表，并询问：
- "识别出以下关键目录：[列表]，是否需要补充？"

用户确认关键目录列表后，再进入下一步。

### 第2步：根目录 AGENTS.md 生成

按以下章节逐一询问：

#### 2.1 项目简介

根据扫描结果，先自行分析并展示项目业务背景的初步理解，然后询问用户：
- "这是我对项目业务背景的初步理解，是否需要修改？"
- "是否有补充？"

用户确认后，严格按照如下模板生成该章节草稿内容
\`\`\`
# 项目简介
**项目简要描述**: 一句话描述项目是做什么的
\`\`\`

#### 2.2 关键架构列表

按照如下模板，生成该章节草稿内容：
\`\`\`
# 关键架构列表
- 文件或目录路径：[简单描述业务背景和功能]
- 文件或目录路径：[简单描述业务背景和功能]
\`\`\`

向用户确认，关键架构列表是否有遗漏，描述是否有误需要修改

#### 2.3 核心开发工作流程

基于项目扫描结果，先自行分析并展示核心开发工作流程的初步理解，然后询问用户：
- "这是我对项目核心开发工作流程的初步理解，是否需要修改？"
- "是否有补充？（例如：基于项目特点，建议添加 xxx 流程）"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# 核心开发工作流程
- 步骤名称：具体描述
\`\`\`

#### 2.4 开发约束

询问用户：
- "请描述项目的技术栈限制、编码规范、部署要求？"
- "是否有补充的约束信息需要添加？"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# 开发约束
- 约束点1: xxxxx
- 约束点2: xxxxx
- 约束点3: xxxxx
\`\`\`

#### 2.5 整合生成根目录 AGENTS.md

在所有章节都得到用户确认后，将内容整合成完整的 AGENTS.md 文件（引用"模板章节 A"），写入项目根目录。

### 第3步：关键目录 AGENTS.md 生成

对于识别出的每个关键目录，按以下章节逐一询问：

#### 3.1 目录功能背景

根据该目录的文件结构和内容，先自行分析并展示该目录功能背景的初步理解（包括判断是业务模块还是公共功能模块），然后询问用户：
- "这是我对该目录功能背景的初步理解（业务模块/公共功能模块），是否需要修改？"
- "是否有补充？"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# [目录相对地址]

**目录简介**: 业务模块/公共功能模块，描述其职责
\`\`\`

#### 3.2 关键架构列表

按照如下模板，生成该章节草稿内容：
\`\`\`
# 关键架构列表
- 文件或目录路径：[简单描述业务背景和功能]
- 文件或目录路径：[简单描述业务背景和功能]
\`\`\`

向用户确认，关键架构列表是否有遗漏，描述是否有误需要修改

#### 3.3 开发约束

询问用户：
- "该目录的技术约束或规范？"
- "该目录的依赖关系或注意事项？"
- "是否有补充的约束信息？"

用户确认后，严格按照如下模板生成该章节内容
\`\`\`
# 开发约束
- 约束点1: xxxxx
- 约束点2: xxxxx
- 约束点3: xxxxx
\`\`\`

#### 3.4 整合生成该目录的 AGENTS.md

在所有章节都得到用户确认后，将内容整合成完整的 AGENTS.md 文件（引用"模板章节 B"），写入该目录。

### 第4步：完成总结

完成所有 AGENTS.md 文件生成后，向用户总结：
- 生成了哪些目录的 AGENTS.md 文件
- 每个文件包含的章节
- 提醒用户可以根据需要手动调整

---

## 更新模式（AGENTS.md 已存在时）

### 第1步：扫描项目内所有 AGENTS.md

扫描项目根目录及所有子目录，找出所有 AGENTS.md 文件路径列表。

向用户展示：
- "检测到以下 AGENTS.md 文件：[列表]"
- "是否需要补充？"

用户确认后，进入下一步。

### 第2步：循环处理每个 AGENTS.md

对列表中的每个 AGENTS.md 文件，执行以下子步骤：

#### 2.1 检查章节完整性

读取该 AGENTS.md 内容，判断其为根目录还是子目录：
- 根目录 AGENTS.md：按"模板章节 A"逐标题匹配
- 子目录 AGENTS.md：按"模板章节 B"逐标题匹配

对比结果：
- **所有章节都存在** → 标记为"无需更新"，跳过 2.2～2.4，进入下一个循环
- **缺少部分章节** → 标记为"需要更新"，进入 2.2

#### 2.2 呈现差异并询问

向用户展示：
- "文件 [路径] 缺少以下章节：[缺失章节列表]"
- "是否需要补充？"

如果用户选择跳过，进入下一个循环。
如果用户确认需要补充，进入 2.3。

#### 2.3 询问补充内容

对每个缺失章节，逐一询问用户：
- "请提供 [章节名] 的内容"
- "是否有补充？"

用户确认每个章节内容后，进入 2.4。

#### 2.4 用户确认后执行更新

向用户呈现更新后的完整文件内容，确认：
- "这是更新后的文件内容，是否确认写入？"

用户确认后，将内容写入文件。

#### 2.5 进入下一个循环

重复 2.1～2.4，直到所有 AGENTS.md 处理完成。

### 第3步：完成总结

向用户展示：
- 更新了哪些 AGENTS.md 文件
- 每个文件更新的内容是什么
- 提醒用户可以手动检查

---

## 重要规则

- 每个章节生成前必须先询问用户，确认后再生成
- 必须等待用户补充信息后再整合内容
- 使用自然语言与用户交互，不要一次性问太多问题
- 如果用户跳过某个章节，直接跳过不要生成
- 文件生成后展示给用户确认内容是否正确
- 更新模式下，必须先扫描出所有 AGENTS.md 才能进入循环
- 循环处理时，必须先呈现差异并得到用户确认，才能执行更新

---

## 模板章节定义（被创建/更新模式引用）

### 模板章节 A：根目录 AGENTS.md 标准章节

包含以下章节：
1. # 项目简介
2. # 关键架构列表
3. # 核心开发工作流程
4. # 开发约束

---

### 模板章节 B：子目录 AGENTS.md 标准章节

包含以下章节：
1. # [目录相对地址] 及 **目录简介**
2. # 关键架构列表
3. # 开发约束
`

const command = {
    description: "深度生成或更新项目根目录及关键目录的 AGENTS.md 文件",
    template,
}

export default command
