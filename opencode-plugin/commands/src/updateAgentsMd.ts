const template = `
# 更新项目 AGENTS.md 文件

## 执行流程

### 第一步：扫描项目中的 AGENTS.md

使用 Bash 工具执行以下命令扫描项目中的所有 AGENTS.md 文件：
- 列出根目录的 AGENTS.md：如果存在，则根目录需要更新
- 递归列出所有 AGENTS.md 文件：使用工具 !glob pattern="**/AGENTS.md"

记录每个 AGENTS.md 文件的路径及其所在目录。

### 第二步：分析变更影响

根据用户描述的本次变更内容，分析哪些 AGENTS.md 需要更新：

**变更类型判断规则**：
- 如果变更涉及**根目录**或**根目录下的文件**，则根目录 AGENTS.md 可能需要更新
- 如果变更涉及**[子目录名]**目录下的文件，则 [子目录名] 的 AGENTS.md 可能需要更新
- 依此类推，变更涉及哪个目录，哪个目录的 AGENTS.md 就可能需要更新

对于每个可能需要更新的 AGENTS.md，确定需要更新的章节：
- **文件/目录变更** → 更新"关键文件快速导航"章节
- **业务需求变更** → 更新"项目业务背景"章节
- **技术约束/规范变更** → 更新"重要约束"章节

### 第三步：呈现分析结果

向用户呈现分析结果，包含：
- 需要更新的 AGENTS.md 文件列表
- 每个文件需要更新的章节及原因
- 询问用户："以上是需要更新的 AGENTS.md 文件，确认要更新吗？"

### 第四步：获取更新内容

用户确认后，针对每个需要更新的 AGENTS.md，逐一询问用户：
- "请描述 [文件名] 的 [章节名] 需要更新什么内容？"
- 用户描述后，系统生成该章节的更新内容

### 第五步：呈现更新后的内容

生成更新后的完整内容，呈现给用户：
- 显示每个 AGENTS.md 文件的更新详情
- 询问用户："以上是更新后的内容，确认写入吗？"

### 第六步：写入文件

用户确认后，使用 Write 工具将更新后的内容写入对应的 AGENTS.md 文件。

### 第七步：完成总结

更新完成后，向用户总结：
- 更新了哪些 AGENTS.md 文件
- 每个文件更新了哪些章节

---

**重要规则**：
- 必须先分析影响，再询问用户确认
- 每次更新前必须先呈现更新内容，得到用户确认后才能写入
- 使用自然语言与用户交互，不要一次性问太多问题
- 如果用户取消，直接结束不写入
`

const command = {
    description: "更新项目中需要变更的 AGENTS.md 文件",
    template,
}

export default command
