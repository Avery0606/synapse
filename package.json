{
  "scripts": {
    "bootstrap": "npm install && cd opencode-plugin && npm install",
    "setup-skills": "node scripts/setup-skills.js"
  },
  "devDependencies": {
    "@types/node": "25.9.1",
    "typescript": "6.0.3"
  }
}
